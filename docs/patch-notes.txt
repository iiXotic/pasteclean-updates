> PasteClean 0.1.5
  Fix: Honor pause/resume state when updating rules from the UI; decode camelCase payloads without losing defaults
  Fix: Prevent duplicate rule syncs on checkbox toggles and surface invoke errors in console
  Fix: Replace non-ASCII markers in history, batch output, and VirusTotal results to avoid mojibake on Windows
  Fix: Normalize release loader, domain rule text, and About “Checking...” message to ASCII
  Change: Added rulesEnabled state seed so tray status persists between sessions
  Change: Bumped all manifests and rebuilt Windows installers for 0.1.5
